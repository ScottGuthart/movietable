export function AuthFooter() {
  return (
    <footer className="flex flex-col items-center gap-2 px-6 py-10 text-center sm:px-8 sm:py-12">
      <p className="text-muted-foreground max-w-md text-xs leading-5 text-pretty sm:text-sm">
        Signing in stores only your ratings and the email address you sign in with. The table stays open to everyone.
      </p>
    </footer>
  )
}
